{
  "name": "app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "npx esbuild src/index.ts --bundle --outdir=dist --platform=node --external:aws-sdk"
  },
  "dependencies": {
    "@aws-sdk/client-appsync": "^3.28.0",
    "isomorphic-fetch": "^3.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.83",
    "@types/node": "^16.7.8",
    "esbuild": "^0.12.24",
    "typescript": "^4.4.2"
  }
}
